/**
 * content-provider — NOT a production replacement yet.
 *
 * This package is the intended future TypeScript/Node.js implementation of
 * Content Provider, meant to gradually take over from
 * packages/legacy-content-provider (the current, actually running .NET +
 * Blazor + assorted experiments implementation). See README.md.
 *
 * Nothing here is implemented yet — only the compatibility model types.
 */

export * from "./types.js";
