{
  "name": "cp-postgre",
  "version": "0.0.0",
  "private": true,
  "description": "PostgreSQL storage implementation of cp-core's ContentProviderStorage. Reads CHAD's cp_items table (Story 80 schema). Optional backend selectable via cp-entry.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cp-core": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.3.3"
  }
}
