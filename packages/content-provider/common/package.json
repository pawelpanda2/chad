{
  "name": "cp-core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Content Provider contracts (lives in packages/content-provider/common/). Models/interfaces/method names only — no storage selection. Consumed by cp-entry, cp-files, cp-mongo, cp-postgre, cp-net-adapter.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
