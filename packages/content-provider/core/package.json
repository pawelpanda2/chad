{
  "name": "cp-core",
  "version": "0.0.0",
  "private": true,
  "description": "Content Provider compatibility models/interfaces/method names only — no storage selection logic. Consumed by cp-entry, cp-files, cp-mongo, cp-net-adapter.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
