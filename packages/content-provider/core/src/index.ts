/**
 * cp-core — models, interfaces, and compatible method names only.
 * Does not select a storage implementation — see cp-entry for that.
 */

export * from "./types.js";
export * from "./contracts.js";
