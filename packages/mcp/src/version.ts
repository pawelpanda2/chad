import { LATEST_PROTOCOL_VERSION } from "@modelcontextprotocol/sdk/types.js";

/** Kept in sync with package.json's own version manually — no build-time JSON import needed for one string. */
export const MCP_PACKAGE_VERSION = "1.0.0";
export const MCP_PROTOCOL_VERSION = LATEST_PROTOCOL_VERSION;
