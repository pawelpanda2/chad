# Story 118 — Local Mac Docker: automatic cp_1 mount & recovery

See user prompt v11 (Cursor — automatyczny mount i recovery cp_1).

Baseline (pre-implementation): `42de334` (folders recursive delete commit);
task kickoff SHA noted earlier as `f9f373d` before that baseline commit.
