# Story 86 — Knowledge

Mockup: `examples/beeper_permissions_mockup_v7.html`

Permission filter:
- All — every contact
- Include — `include === true`
- Exclude — `exclude === true`
- Permission — `include && !exclude`

Checkboxes mutually exclusive; both may be unchecked.
