# Story 108 — Input

Msg Workout → MANUALLY ADDED MESSAGES: master/detail page to assign
WhatsApp export archives (.rar / .zip) to leads on the shared `cp_1`
volume under `02_files_refrenced/<sessionUser>/02_files_zip`. No unpack/
parse. Reuse Photos storage mount + sidecar metadata pattern.

Start SHA: `336ebc4` (parallel WIP left untouched: Story 107, photos
galleries, gui-standard, msg-automation hub edits, local cp1 scripts).
