# Story 93 — Others / deferred

- Per-user audio subfolders (if isolation of shared `10_files_audio` is
  required later).
- Recording list / delete UI.
- Whisper / transcription.
- MP3 transcoding.
- Optional: mount audio dir on every historical compose variant if any
  were missed.
