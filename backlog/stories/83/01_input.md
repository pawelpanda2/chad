# Story 83 — Input

## Input 1

(sent mid-session, interrupting Story 82 work)

nei musisz juz testowac
ale potrzebuje inny wazny tutaj feature
musze miec pewnosc ze jestem podpiety pod baze servera(qnap) a nie lokalna
to ustawienie jest w config
stworz mi w dev panel zakladke settings i w niej combobox zebym mogl wybrac baze z ktorej korzystam
przerwij poprzednie zadanie bo jest dobrze wykonane i szkoda tokenow

## Input 2

(clarifying question asked back: process env vars like DBA_MONGO_MODE/DBA_PRIMARY_BACKEND are set once at process start and can't safely be live-switched without a restart — what should the combobox actually do: read-only display of the currently active source, or a real live switch?)

Answer: "Realne przełączanie" (real switching).
